const array = [];
let max = array[0];
let index = 0;

while (index < array.length) {
    if (array[index] > max) {
        max = array[index];
    } else {

    }
    index = index + 1;
}

console.log(max);